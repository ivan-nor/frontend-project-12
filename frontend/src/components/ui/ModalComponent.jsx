/* eslint-disable react/prop-types */
import { Modal, Col, Row } from 'react-bootstrap'

const ModalComponent = ({ isShow, handleChange, handleSubmit, channel }) => {
  return isShow && (
    <Modal>
      channel.id
    </Modal>
  )
}

export default ModalComponent
